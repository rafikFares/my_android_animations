<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myanimations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainTransitionManagerFirst">

        </activity>

        <activity android:name=".MainTransitionManagerSeconde" />

        <activity android:name=".MainAnimationDrawable" >

        </activity>

        <activity android:name=".MainAnimationVectorDrawable" >

        </activity>

        <activity android:name=".MainAnimationObjectAnimator" >

        </activity>

        <activity android:name=".MainAnimationCoordinatorLayout" >

        </activity>

        <activity android:name=".MainMotionLayout" >

        </activity>

        <activity android:name=".MainMotionLayoutWithConstraintSet" >

        </activity>

        <activity android:name=".MainOverridedMotionLayoutWithCoordinator" >

        </activity>

        <activity android:name=".MainOverridedMotionLayoutWithDrawer" >

        </activity>

        <activity android:name=".MainOverridedMotionLayoutWithViewPager" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>